[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.0.0", "pnpm install --filter api --frozen-lockfile=false"]

[phases.build]
cmds = ["pnpm --filter api run build"]

[start]
cmd = "cd apps/api && node dist/index.js"