[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/api && npx prisma generate && pnpm --filter api run build"]

[start]
cmd = "cd apps/api && node dist/index.js"
